apiVersion: v1
kind: Service
metadata:
  name: dockerfile-app-service
spec:
  selector:
    app: dockerfile-app-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
  type: NodePort
  